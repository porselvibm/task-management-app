Task manager App built with the following requirements:

React.js and its hooks API

React router

Material UI

React Testing Library

Jest

LocalStorage to avoid data disappearing after reload

yarn package manager
